#include "CombatHelpers.h"

unsigned CombatHelpers::CalculateAbilityDamage(RawPokemonData attacker, RawPokemonData defender, MoveData data)
{
    /*if(Data.MoveType MOVETYPE_STATUS)
    {
        return 0;
    }
    float AttackMultiplier = 1; // this should be queried against an effectiviness table;
    float EffectiveAttackStat = (Data.MoveType MOVETYPE_STATUS)? 
    float EffectiveDefenceStat = 
    float Damage = (2 * Attacker.Level/5);
    Damage *= Data.Power * ()*/
    return 0;
    
}
